<div>
  <% @tags.each do |tag| %>
    <%= link_to posts_path(tag: tag.name) do %>
      <span class="inline-block py-1 px-2 border rounded-full text-indigo-500 text-xs font-medium tracking-widest   hover:bg-indigo-500 hover:text-white"><%= "##{tag}" %></span>
    <% end %>
  <% end %>
</div>
