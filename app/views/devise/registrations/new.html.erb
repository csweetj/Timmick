<% content_for :devise_form do %>

  <h2 class="pt-4 mb-8 text-4xl font-bold heading">ユーザー登録</h2>

  <%= form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f| %>
    <%= render "devise/shared/error_messages", resource: resource %>

    <div class="input-group">
      <%= f.label :ニックネーム, class:"label" %>
      <%= f.text_field :nickname, class:"input" %>
    </div>

    <div class="input-group">
      <%= f.label :メールアドレス, class:"label" %>
      <%= f.email_field :email, autocomplete: "email", class:"input" %>
    </div>

    

    <div class="input-group">
      <div class="flex">
        <%= f.label :パスワード（最低６文字）, class: "label" %>
      </div>
      <%= f.password_field :password, autocomplete: "new-password", class: "input" %>
    </div>

    <div class="input-group">
      <%= f.label :パスワード（確認）, class:"label" %>
      <%= f.password_field :password_confirmation, autocomplete: "new-password", class: "input" %>
    </div>

    <div class='input-group'>
      <%= f.label :誕生日, class:"label" %>
      <div class="flex">
        <%= raw sprintf(
                    f.date_select(
                      :birthday,
                      class:'select-birth',
                      id:"birth-date",
                      use_month_numbers: true,
                      prompt:'--',
                      start_year: 1930,
                      end_year: (Time.now.year - 1),
                      date_separator: '%s'),
                    "<p> 年 </p>", "<p> 月 </p>") + "<p> 日 </p>" %>
      </div>
    </div>

    <div class="input-group">
    <%= f.label :職業, class:"label" %>
    <%= f.collection_select(:career_id, Career.all, :id, :name, {}, {class:""}) %>
    </div>
    
    <div class="input-group">
    <%= f.label :性別, class:"label" %>
    <%= f.collection_select(:gender_id, Gender.all, :id, :name, {}, {class:""}) %>
    </div>
    
    <div class="input-group">
      <%= f.submit "登録", class: "btn btn-default" %>
    </div>

    <hr class="mt-6 border" />

  <% end %>

  <%= render "devise/shared/links" %>

<% end %>

<%= render "devise/shared/form_wrap" %>
